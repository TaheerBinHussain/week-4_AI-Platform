apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: grafana
  namespace: flux-system
spec:
  interval: 10m
  path: ./flux/infra/observability/grafana
  prune: true
  sourceRef:
    kind: GitRepository
    name: sovereign-ai-platform
  targetNamespace: observability
  dependsOn:
    - name: prometheus
    - name: loki
    - name: tempo
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: grafana
      namespace: observability
