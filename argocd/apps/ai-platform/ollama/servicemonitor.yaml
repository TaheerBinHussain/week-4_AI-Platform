apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ollama
  namespace: ai-platform
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ollama
  endpoints:
    - port: http
      path: /metrics
